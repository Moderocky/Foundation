/**
 * The class file assembler.
 * The elements in this class are based exactly on the virtual machine specification.
 * Although type names have been adjusted (u1 -> U1, utf8_info -> Utf8Info) the method and field names
 * have been preserved exactly as written in the specification.
 */
package mx.kenzie.foundation.assembler;